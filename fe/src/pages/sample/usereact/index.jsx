import UI from "@component/gip-ui";
import usereact from "gh-usereact";

export default function App(props) {
  return (
    <UI.Stack>
      <UI.Text variant="body1">This is use react</UI.Text>
    </UI.Stack>
  );
}
